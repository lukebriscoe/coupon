.env
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
venv/
.venv/

# Runtime state, local to each install.
data/cache/
data/api_usage.json

# data/slips.json IS tracked: the scheduled workflow commits it back so the
# history and performance pages survive between runs. It is published on the
# site anyway, so there is nothing kept private by ignoring it.
_site/
